/**
 * Admin Internal Panel — Phase 4+
 * For creating events, invitations, controlled links, and managing guest lists.
 */
export default function AdminPage() {
  return (
    <main className="min-h-dvh flex items-center justify-center bg-black text-white">
      <div className="flex flex-col items-center gap-4 text-center px-6">
        <h1 className="text-2xl font-bold" style={{ color: "#C9A24D" }}>
          لوحة الإدارة الداخلية
        </h1>
        <p className="text-sm text-gray-500">Admin Panel — المرحلة 4+</p>
      </div>
    </main>
  );
}
